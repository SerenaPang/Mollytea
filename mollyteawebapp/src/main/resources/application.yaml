spring:
  application:
    name: mollyteawebapp
    
  datasource:
    url: jdbc:mysql://localhost:3306/milktea
    username: root
    password: 123456

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  thymeleaf:
    cache: false

server:
  port: 8080